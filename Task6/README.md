# Task6 — Классификация данных (движок перед аналитическим хранилищем)

## Состав

- `c2-classification-engine.drawio.xml` — диаграмма C2 (контейнеры решения) движка классификации перед пакетной загрузкой в аналитическое хранилище.
- `design-notes.md` — пояснения: как переживаем schema drift, какие слои в хранилище, метрики качества классификации, scalability.
